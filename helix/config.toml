theme="ayu_dark"

[editor]
idle-timeout = 200

[editor.file-picker]
hidden = false

[editor.soft-wrap]
enable = true

[keys.normal]
C-r = ":pipe-to kitty @ send-text --match recent:1 --stdin"
C-A-l = ":format"

[keys.select]
C-r = ":pipe-to kitty @ send-text --match recent:1 --stdin"

function setup_git_rs --description 'set git details to rootsys for project'
    git config user.name "Alex Schlögl"
    git config user.email "a.schloegl@rootsys.at"
end
    
function setup_git_priv --description 'set git details to private for project'
    git config user.name "Alex Schlögl"
    git config user.email "alxshine@proton.me"
end
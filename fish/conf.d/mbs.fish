function mbs --wraps='mbot send --config-path ~/.mbot_config.json' --description 'alias mbs mbot send --config-path ~/.mbot_config.json'
  mbot send --config-path ~/.mbot_config.json $argv
        
end
